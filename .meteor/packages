# Meteor packages used by this project, one per line.
# Check this file (and the other files in this directory) into your repository.
#
# 'meteor add' and 'meteor remove' will edit this file for you,
# but you can also edit it by hand.

meteor-base@1.0.4             # Packages every Meteor app needs to have
mobile-experience@1.0.4       # Packages for a great mobile UX
mongo@1.1.14                   # The database Meteor supports right now
blaze-html-templates@1.0.4 # Compile .html files into Meteor Blaze views
reactive-var                # Reactive variable for tracker
jquery@1.11.10                  # Helpful client-side library
tracker@1.1.1                 # Meteor's client-side reactive programming library

standard-minifier-css@1.3.2   # CSS minifier run for production mode
standard-minifier-js@1.2.1    # JS minifier run for production mode
es5-shim@4.6.15                # ECMAScript 5 compatibility for older browsers.
ecmascript@0.5.9              # Enable ECMAScript2015+ syntax in app code
shell-server@0.2.1            # Server-side component of the `meteor shell` command

#autopublish@1.0.7             # Publish all data to the clients (for prototyping)
#insecure@1.0.7                # Allow all DB writes from clients (for prototyping)


#stolinski:stylus-multi      #video guy prefers to work in stylus - Meteor hung when Ty Short installed this.
mquandalle:stylus           #alternative in case the stolinski doesn't work.  THIS ONE KIND OF WORKS.  PADDING IN TEMPLATE DOESN'T WORK.



kadira:flow-router          #sets up routes
kadira:blaze-layout
coffeescript@1.11.1_3
cosmos:browserify
erasaur:meteor-lodash
fortawesome:fontawesome     #beautiful buttons
spiderable@1.0.14-release-testing.0
fastclick@1.0.13
dburles:collection-helpers
raix:handlebar-helpers      #"nice way to use session variables in templates"
aldeed:collection2          #allows us to attach schemas to things (contains aldeed:simple-schema)
simple:json-routes
aldeed:autoform             #generates forms
accounts-ui@1.1.9                 #standard account setup (used in Ty Short's first app, yay!)
accounts-password@1.3.2           #standard account setup
matb33:bootstrap-glyphicons
#msavin:jetsetter            #this one replaced with meteortoys:allthings below which contains this.  Scott was having problems with this in the early videos.
zimme:active-route
gwendall:auth-client-callbacks  #used for redirecting user on login
meteortoys:allthings         #added later in the file (jetsetter is included).  USED ON EVERY ONE OF SCOTT'S WEB PROJECT.
datariot:ganalytics         #google analytics
npm-bcrypt@0.9.2        #used in the userinterface accounts
check               #used to check if a variable type, such as check(id, String) as used in publish.js
